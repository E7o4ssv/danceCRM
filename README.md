# Dance School CRM

Система управления школой танцев с полным функционалом для администраторов и преподавателей.

## 🎯 Возможности

### 👥 Управление пользователями
- **Администраторы** - полный доступ ко всем функциям
- **Преподаватели** - доступ к группам, студентам и посещаемости
- Система аутентификации с JWT токенами

### 📚 Управление группами
- Создание и редактирование групп
- Добавление/удаление студентов и преподавателей
- Настройка расписания занятий
- Групповой чат для общения

### 👨‍🎓 Управление студентами
- База данных студентов с контактной информацией
- История посещаемости
- Индивидуальные уроки
- Привязка к группам

### 📊 Посещаемость
- Отметка присутствия студентов
- Статистика посещаемости
- Отчеты по группам и периодам

### 💬 Чат система
- **Приватные сообщения** между пользователями
- **Групповой чат** для общения в группах
- Real-time обновления через Socket.io
- Поиск по сообщениям и пользователям

### 📅 Индивидуальные уроки
- Планирование персональных занятий
- Заметки и комментарии
- Отслеживание прогресса

## 🛠 Технологии

### Backend
- **Node.js** + **Express.js**
- **MongoDB** с **Mongoose**
- **JWT** аутентификация
- **Socket.io** для real-time чата
- **Swagger** документация API

### Frontend
- **React.js** с **Hooks**
- **Tailwind CSS** для стилизации
- **React Router** для навигации
- **Axios** для API запросов
- **React Icons** для иконок

### Развертывание
- **PM2** для управления процессами
- **Nginx** для статических файлов
- **Docker** готовность

## 🚀 Быстрый старт

### Установка зависимостей
```bash
# Backend
cd server
npm install

# Frontend
cd client
npm install
```

### Настройка окружения
```bash
# Создайте .env файл в корне проекта
cp env.example .env

# Заполните переменные окружения
MONGODB_URI=mongodb://localhost:27017/dance-school
JWT_SECRET=your-secret-key
PORT=5000
```

### Запуск в разработке
```bash
# Backend (порт 5000)
cd server
npm run dev

# Frontend (порт 3000)
cd client
npm start
```

### Сборка для продакшена
```bash
# Frontend
cd client
npm run build

# Backend
cd server
npm start
```

## 📁 Структура проекта

```
dance/
├── client/                 # React frontend
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── contexts/       # React контексты
│   │   ├── hooks/          # Кастомные хуки
│   │   ├── utils/          # Утилиты
│   │   └── styles/         # CSS стили
│   └── public/             # Статические файлы
├── server/                 # Node.js backend
│   ├── models/             # MongoDB модели
│   ├── routes/             # API роуты
│   ├── middleware/         # Express middleware
│   └── index.js           # Главный файл сервера
├── data/                   # Данные для инициализации
└── docs/                   # Документация
```

## 🔌 API Endpoints

### Аутентификация
- `POST /api/auth/login` - вход в систему
- `POST /api/auth/register` - регистрация
- `GET /api/auth/profile` - профиль пользователя

### Группы
- `GET /api/groups` - список групп
- `POST /api/groups` - создание группы
- `PUT /api/groups/:id` - обновление группы
- `DELETE /api/groups/:id` - удаление группы

### Студенты
- `GET /api/students` - список студентов
- `POST /api/students` - создание студента
- `PUT /api/students/:id` - обновление студента
- `DELETE /api/students/:id` - удаление студента

### Посещаемость
- `GET /api/attendance` - отчеты по посещаемости
- `POST /api/attendance` - отметка посещаемости
- `PUT /api/attendance/:id` - обновление отметки

### Чат
- `GET /api/private-chat/chats` - список приватных чатов
- `GET /api/private-chat/users` - список пользователей
- `POST /api/private-chat/chats/:partnerId/messages` - отправка сообщения
- `GET /api/chat/group/:groupId` - групповой чат

## 🎨 UI/UX Особенности

### Дизайн
- **Glassmorphism** эффекты
- **Gradient** фоны
- **Анимации** и переходы
- **Адаптивный** дизайн
- **Темная** тема

### Компоненты
- **Модальные окна** для форм
- **Уведомления** и алерты
- **Загрузочные** индикаторы
- **Поиск** и фильтрация
- **Пагинация** для больших списков

## 🔒 Безопасность

- **JWT** токены для аутентификации
- **Role-based** авторизация
- **Валидация** данных на сервере
- **CORS** настройки
- **Helmet** для безопасности HTTP заголовков

## 📊 Мониторинг

- **PM2** для управления процессами
- **Логирование** ошибок
- **Health checks** для API
- **Метрики** производительности

## 🚀 Развертывание

### Автоматическое развертывание
```bash
# Используйте скрипт для развертывания
./deploy-final.sh
```

### Ручное развертывание
```bash
# 1. Сборка frontend
cd client && npm run build

# 2. Копирование файлов на сервер
scp -r client/build/* user@server:/path/to/app/

# 3. Перезапуск backend
ssh user@server "pm2 restart dance-school-api"
```

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📝 Лицензия

Этот проект лицензирован под MIT License.

## 📞 Поддержка

Если у вас есть вопросы или предложения, создайте issue в репозитории.

---

**Dance School CRM** - современная система управления школой танцев с полным функционалом для эффективной работы администраторов и преподавателей. 